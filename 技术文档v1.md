# 技术文档 V1 (Technical Document V1.0)

## 1. 系统架构与技术栈规划

本系统定位为轻量化的 Web 端应用，前后端分离架构，专注于核心的岗位投递跟进和面经复盘。不包含冗余的第三方抓取、复杂的AI规划功能。

### 1.1 技术栈选型
*   **后端开发语言与框架**：Java 17, Spring Boot 3.x
*   **持久层框架**：MyBatis-Plus (或 Spring Data JPA，推荐 MB+ 便于单表快速CRUD)
*   **关系型数据库**：MySQL 8.0
*   **缓存与Session状态**：Redis (用于 Token 存储和验证码/简单高频缓存)
*   **前端框架**：Vue.js 3 (Composition API) + Element Plus 或 React + Ant Design (推荐 Vue 用于轻量级中后台)
*   **构建与依赖管理**：Maven
*   **接口规范**：RESTful API (使用 Swagger / SpringDoc 自动生成接口文档)

---

## 2. 数据库核心模型设计 (Schema Design)

为了支撑 V1.0 版本核心功能（用户系统、岗位台账、面试复盘），规划以下核心数据表：

### 2.1 用户表 (`sys_user`)
存储用户的基本认证与配置信息。
*   `id` (BIGINT, PK): 用户唯一标识
*   `username` (VARCHAR): 登录用户名
*   `password` (VARCHAR): 密码(BCrypt加密)
*   `create_time` (DATETIME): 账号创建时间
*   `update_time` (DATETIME): 账号更新时间

### 2.2 岗位投递台账表 (`job_application`)
投递平台的核心台账表。
*   `id` (BIGINT, PK): 记录ID
*   `user_id` (BIGINT, FK): 关联用户ID (保证数据私有化)
*   `company_name` (VARCHAR): 公司名称 [必填]
*   `job_title` (VARCHAR): 岗位名称 [必填]
*   `channel` (VARCHAR): 投递渠道 (如: Boss直聘, 牛客, 官网) [必填]
*   `status` (TINYINT): 当前状态 (0:待投递, 1:已投递, 2:简历初筛, 3:笔试, 4:一面, 5:二面, 6:终面, 7:Offer, 8:落选/放弃) [必填]
*   `apply_time` (DATETIME): 投递时间 [必填]
*   `location` (VARCHAR): 工作地点
*   `salary_range` (VARCHAR): 薪资范围
*   `job_type` (TINYINT): 岗位类型 (0:实习, 1:校招, 2:社招)
*   `priority` (TINYINT): 优先级 (0:低, 1:中, 2:高)
*   `hr_contact` (VARCHAR): HR联系方式
*   `is_deleted` (TINYINT): 逻辑删除标志
*   `create_time` (DATETIME)
*   `update_time` (DATETIME) 

### 2.3 面试及复盘记录表 (`interview_record`)
关联到投递台账，支持单次投递的多轮面试记录和复盘。
*   `id` (BIGINT, PK)
*   `application_id` (BIGINT, FK): 关联岗位投递表 ID
*   `record_type` (TINYINT): 记录类型 (1:笔试, 2:面试)
*   `schedule_time` (DATETIME): 日程时间 (笔试/面试开始时间)
*   `core_questions` (TEXT): 核心题目/面试提问
*   `summary` (TEXT): 复盘总结 (不足、后续优化)
*   `fail_reason` (VARCHAR): 挂掉原因 (仅当该轮未通过时填写)
*   `create_time` (DATETIME)

---

## 3. 核心 API 接口设计规划 (RESTful)

### 3.1 认证与用户模块
*   `POST /api/v1/auth/login`: 用户登录，成功后返回 JWT Token
*   `POST /api/v1/auth/register`: 账号注册
*   `GET /api/v1/user/info`: 获取当前登录用户信息
*   `POST /api/v1/user/clear-data`: 清空当前用户所有投递数据

### 3.2 岗位台账模块
*   `POST /api/v1/applications`: 新增岗位投递 (后端做同公司同岗位90天去重校验)
*   `PUT /api/v1/applications/{id}`: 编辑单个岗位详情
*   `DELETE /api/v1/applications/{id}`: 删除投递记录
*   `PUT /api/v1/applications/{id}/status`: 快捷更新岗位状态
*   `GET /api/v1/applications`: 分页查询列表，支持多种传参 (关键词搜索、状态、时间排序等)
*   `POST /api/v1/applications/import`: 上传 Excel 批量导入
*   `GET /api/v1/applications/export`: 导出当前条件下的 Excel 文件

### 3.3 面试复盘与提醒模块
*   `POST /api/v1/interviews`: 为对应 application_id 新增面试/笔试记录与日程安排
*   `GET /api/v1/interviews/application/{id}`: 查询某条投递记录下的所有节点轨迹与复盘
*   `GET /api/v1/reminders/upcoming`: 获取近期(如未来3天内)的笔试/面试提醒列表
*   `GET /api/v1/reminders/timeout`: 获取长久(超7天)未流转的 timeout 岗位列表

### 3.4 极简数据统计模块
*   `GET /api/v1/statistics/summary`: 核心指标概览 (总投递、各状态计数)
*   `GET /api/v1/statistics/funnel`: 获取投递转化漏斗数据 (初筛通过率、面试率、Offer转化率)
*   `GET /api/v1/statistics/channels`: 各投递渠道投递数量占比分组

---

## 4. 关键业务逻辑实现指导

### 4.1 "防重复投递" 校验逻辑
在执行 `POST /api/v1/applications` 时：
1. 提取当前 `user_id`, `company_name`, `job_title` 和 `apply_time`。
2. 查询数据库中是否存在 `user_id` 相同，且 `company_name` 匹配，`job_title` 相等，且投递时间在 `apply_time` 往前推算 90 天内的记录。
3. 若存在命中记录，由接口抛出 `RepeatApplicationException`，前端拦截此异常并弹窗提示用户（由于此行为只是警告，视产品需求可提供“强制覆盖/录入”参数）。

### 4.2 超时提醒机制
*   在拉取 `GET /api/v1/reminders/timeout` 或者首页时动态计算。
*   对于状态属于“进行中”（如已投递、初筛、各轮面试阶段）的记录，用当前系统时间减去 `update_time`（最后一次状态流转更新的时间）。若间隔的自然日天数 > 设定阈值（如7/14天），则标记为超时未跟进岗位。

### 4.3 安全限制 (Private Data)
*   任何对于 `JobApplication` 和 `InterviewRecord` 的增删改查 API，其 Mapper/SQL 必须带上 `WHERE user_id = #{currentUserId}`。
*   通过 AOP 切面拦截带有 Token 的请求，在 RequestContextThreadLocal 中存取 `user_id`，杜绝垂直越权和水平越权漏洞。

## 5. 项目构建部署指引

1. **后端**：使用 `mvn clean package` 构建可执行的 `jar` 包。
2. **环境需要**：JDK 17+, MySQL 8, Redis。
3. **接口调试**：本地运行后访问 `http://localhost:8080/swagger-ui.html` 调测所有 API。
4. **前端部署**：Vue 项目通过 `npm run build` 打包 `dist` 静态产物，部署于 Nginx 即可，Nginx 做 `/api` 转发反向代理至后端的 8080 端口。